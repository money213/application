server:
  port: 8080
spring:
  rabbitmq:
    host: 47.115.212.214
    port: 5672
    username: guest
    password: guest
  application:
    name: order-server
  aop:
    proxy-target-class: true
  sleuth:
    feign:
      enabled: false
    sampler:
      percentage: 0.2
  zipkin:
    base-url: http://47.115.212.214:9411/
  cloud:
    config:
      server:
        git:
          uri: https://github.com/money213/mytest2.git
          username: 914524629@qq.com
          password: lwh463243964
          search-paths: /
          default-label: master
    bus:
      refresh:
        enabled: true
#      trace:
#        enabled: true
    consul:
      retry:
        initial-interval: 10000
#      host: 172.21.0.5
      host: 39.108.63.204
      config:
        enabled: false
#      port: 8510
      port: 8500
      discovery:
        register: true
        prefer-ip-address: true
        heartbeat:
          enabled: true
        acl-token: bd63ce16-ccd0-22e1-d37d-fb948232cbf5
        instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
        service-name: ${spring.application.name}
        host-name: localhost
        port: ${server.port}
        health-check-url: http://${spring.cloud.client.ip-address}:${server.port}/actuator/health
        health-check-interval: 4s
        health-check-critical-timeout: 20s
        tags: foo=bar, baz
  datasource:
    url: jdbc:mysql://39.108.63.204:3307/test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true&verifyServerCertificate=false&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    connection-init-sqls: set names utf8mb4
    username: root
    password: 123456
    druid:
      connection-init-sqls: set names utf8mb4
      initial-size: 10
      max-active: 50
      min-idle: 10
      max-wait: 60000
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      validation-query: SELECT 1 FROM DUAL
      test-on-borrow: false
      test-on-return: false
      test-while-idle: true
      remove-abandoned: true
      remove-abandoned-timeout: 1800
      time-between-eviction-runs-millis: 60000
      filters: stat,wall
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cn.girlbdf.www.order.db.*.**.model,com.baidu.fsg.uid.worker.entity
  configuration:
##开启驼峰uName自动映射到u_name
    map-underscore-to-camel-case: true